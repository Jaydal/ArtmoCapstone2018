<div id="post">
    <form>
        @*<input  class="form-control-file" type="file" id="file" name="file" />*@
        <hr />
        @{
            System.IO.DirectoryInfo dir = new System.IO.DirectoryInfo(Server.MapPath("~/_Storage/backup"));
            foreach (System.IO.FileInfo f in dir.GetFiles("*.*"))
            {
        <div class="form-group">
            <div class="row">
                <div class="col">
                    
                    <label>@f.Name</label>
                </div>
                <div class="col">
                    @Html.ActionLink("restore", "_RestoreFile", new { filename = f.Name }, new { @class = "material-icons text-black", title = "Restore Data" })
                    @Ajax.ActionLink("delete_outline", "DeleteFile", new { filename = f.Name }, new AjaxOptions()
               {
                   HttpMethod = "POST",
                   InsertionMode = InsertionMode.Replace,
                   UpdateTargetId = "post"
               },  new { @class = "material-icons text-black", title = "Delete Data" })
                </div>
            </div>  
        </div>
       
                }
            }
        </form>
</div>
